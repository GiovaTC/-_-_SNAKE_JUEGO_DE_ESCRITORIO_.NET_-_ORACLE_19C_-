# -_-_SNAKE_JUEGO_DE_ESCRITORIO_.NET_-_ORACLE_19C_- :.  
# 🐍 SNAKE – JUEGO DE ESCRITORIO (.NET + ORACLE 19c):

<img width="1024" height="1024" alt="image" src="https://github.com/user-attachments/assets/452dad59-4829-4d07-8a86-14edf0396826" />  

Implementación **completa y profesional** de un juego **Snake en C# (.NET 6)** usando **Visual Studio 2022**,  
con **registro de resultados en Oracle 19c mediante Stored Procedure (SP)**.

Enfoque **100% académico / portafolio**, con arquitectura clara, codigo limpio y persistencia real .

---

## 📌 Características:

- 🎮 Snake clásico (movimiento, colisiones, comida)
- ⏱️ Puntaje automático
- 🧠 Lógica desacoplada
- 🗄️ Persistencia real en Oracle 19c
- ⚙️ Inserción mediante Stored Procedure
- 🧱 Listo para Visual Studio 2022

---

## 🧩 Tecnologías utilizadas

| Componente        | Tecnología        |
|------------------|------------------|
| Lenguaje          | C#               |
| Framework         | .NET 6           |
| UI                | Windows Forms    |
| Base de Datos     | Oracle 19c       |
| Acceso a Datos    | ODP.NET          |
| Persistencia      | Stored Procedure |

---

## 📁 Estructura del proyecto:

```text
SnakeGame/
│
├── Program.cs
├── MainForm.cs
├── GameEngine.cs
├── Snake.cs
├── Food.cs
├── Db/
│   ├── OracleConnectionFactory.cs
│   └── ScoreRepository.cs
└── Models/
    └── GameScore.cs

🗄️ 1️⃣ Base de Datos – Oracle 19c
📌 Tabla de puntajes
CREATE TABLE GAME_SCORES (
    ID          NUMBER GENERATED BY DEFAULT AS IDENTITY,
    PLAYER_NAME VARCHAR2(50),
    SCORE       NUMBER,
    PLAY_DATE   DATE DEFAULT SYSDATE,
    CONSTRAINT PK_GAME_SCORES PRIMARY KEY (ID)
);

📌 Stored Procedure (SP)
CREATE OR REPLACE PROCEDURE SP_INSERT_SCORE (
    P_PLAYER_NAME IN VARCHAR2,
    P_SCORE       IN NUMBER
)
AS
BEGIN
    INSERT INTO GAME_SCORES (PLAYER_NAME, SCORE)
    VALUES (P_PLAYER_NAME, P_SCORE);

    COMMIT;
END;
/

🔌 2️⃣ Conexión Oracle (ODP.NET)
📄 OracleConnectionFactory.cs
using Oracle.ManagedDataAccess.Client;

namespace SnakeGame.Db
{
    public static class OracleConnectionFactory
    {
        private const string CONNECTION_STRING =
            "User Id=SYSTEM;Password=oracle;Data Source=localhost:1521/XEPDB1";

        public static OracleConnection GetConnection()
        {
            return new OracleConnection(CONNECTION_STRING);
        }
    }
}

📦 NuGet requerido
Oracle.ManagedDataAccess
💾 3️⃣ Repositorio – Llamada al Stored Procedure
📄 ScoreRepository.cs
using Oracle.ManagedDataAccess.Client;
using SnakeGame.Models;

namespace SnakeGame.Db
{
    public class ScoreRepository
    {
        public void SaveScore(GameScore score)
        {
            using var conn = OracleConnectionFactory.GetConnection();
            using var cmd = new OracleCommand("SP_INSERT_SCORE", conn);

            cmd.CommandType = System.Data.CommandType.StoredProcedure;
            cmd.Parameters.Add("P_PLAYER_NAME", OracleDbType.Varchar2)
                           .Value = score.PlayerName;
            cmd.Parameters.Add("P_SCORE", OracleDbType.Int32)
                           .Value = score.Score;

            conn.Open();
            cmd.ExecuteNonQuery();
        }
    }
}

🧠 4️⃣ Modelo
📄 GameScore.cs
namespace SnakeGame.Models
{
    public class GameScore
    {
        public string PlayerName { get; set; }
        public int Score { get; set; }
    }
}

🎮 5️⃣ Lógica del Juego
📄 GameEngine.cs
using System.Drawing;

namespace SnakeGame
{
    public class GameEngine
    {
        public int Score { get; private set; } = 0;
        public bool GameOver { get; private set; }

        public void EatFood()
        {
            Score += 10;
        }

        public void EndGame()
        {
            GameOver = true;
        }
    }
}

🖥️ 6️⃣ Formulario Principal (Snake + Guardado)
📄 MainForm.cs (fragmento clave)
using SnakeGame.Db;
using SnakeGame.Models;
using System.Windows.Forms;

namespace SnakeGame
{
    public partial class MainForm : Form
    {
        private GameEngine game = new GameEngine();

        private void GameOver()
        {
            game.EndGame();

            string player = Microsoft.VisualBasic.Interaction.InputBox(
                "Nombre del jugador:", "Game Over");

            var repo = new ScoreRepository();
            repo.SaveScore(new GameScore
            {
                PlayerName = player,
                Score = game.Score
            });

            MessageBox.Show("Puntaje guardado en Oracle 19c");
        }
    }
}

▶️ Flujo completo del sistema
Jugador juega
      ↓
Score aumenta
      ↓
Colisión → Game Over
      ↓
Ingreso nombre
      ↓
Stored Procedure
      ↓
Oracle 19c

🏆 Buenas prácticas aplicadas

✔ Separación UI / lógica / datos

✔ Stored Procedure (sin SQL embebido)

✔ Conexiones controladas

✔ Código escalable

✔ Ideal para proyecto final o portafolio

🚀 Posibles mejoras

Ranking Top 10

Niveles de velocidad

Sonido

Autenticación de usuarios

Gráficos con PictureBox

Reportes Oracle / .
